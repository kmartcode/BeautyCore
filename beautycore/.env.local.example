# ─── Database ───────────────────────────────────────────────────────────────
# Neon PostgreSQL connection string.
# Get one free at https://neon.tech — create a project, copy the pooled
# connection string from the dashboard.
DATABASE_URL=postgresql://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require


# ─── AI: photo analysis (required) ──────────────────────────────────────────
# Google Gemini API key — powers the AI Advisor's vision analysis.
# Free tier is sufficient. Get one at https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Optional. Defaults to gemini-flash-latest, an alias Google keeps pointed at a
# current model. Several gemini-2.5-* ids are closed to new keys and return 404,
# so prefer the alias unless you have a specific reason not to.
# GEMINI_MODEL=gemini-flash-latest


# ─── Sessions (required) ────────────────────────────────────────────────────
# Signing key for the JWT session cookie. Generate a fresh one with:
#   node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
SESSION_SECRET=generate_a_long_random_string_here


# ─── AI: preview generation (optional) ──────────────────────────────────────
# Which provider renders style previews on the client's photo.
#   stub    (default) — no previews; analysis still works and the UI explains why
#   lora              — your own LoRA served over HTTP (see scripts/BeautyCore_LoRA_Colab.ipynb)
#   gemini            — Google image models; REQUIRES a billed Google Cloud account
#                       (free tier returns 429 with quota limit: 0)
PREVIEW_PROVIDER=stub

# Required when PREVIEW_PROVIDER=lora.
# The tunnel URL printed by the last cell of scripts/BeautyCore_LoRA_Colab.ipynb.
# This changes every time the Colab notebook restarts — re-paste it each session.
# LORA_ENDPOINT_URL=https://your-tunnel.trycloudflare.com

# Which style categories the loaded LoRA is actually trained for.
# Defaults to nails, because that is the model that exists. A nails LoRA will
# happily paint nail art onto hair, so out-of-scope requests are refused with
# an honest message instead. Set to "hair,nails" once both models are trained.
# LORA_STYLE_SCOPE=nails

# How strongly the LoRA is applied: 0 = base model, 1 = full strength.
# Defaults to 0.8. Lower it if previews look over-stylised or lose the client's
# real hand; raise it if the trained style barely shows.
# LORA_SCALE=0.8

# Nail previews are rendered as masked inpainting: Gemini locates each nail, and
# only those regions get repainted. Without a mask the model spreads its changes
# over the whole frame — and since a nail is about 1% of a hand photo, it
# restyles the rings and background while leaving the nails alone.
#
# Set to 0 to force the old unmasked path, which is useful for seeing the
# difference. Detection failing (no API key, quota, no nails found) falls back
# to unmasked automatically, so this is only for deliberate comparison.
# LORA_NAIL_MASK=1

# How much of each nail the model may repaint, 0-1. Only ever applies inside the
# mask, which is why it can be near the maximum — that is what makes a real
# colour change possible. Defaults to 0.95. Lower it towards 0.7 if the nail
# shape drifts too far from the client's own.
# LORA_MASK_STRENGTH=0.95

# Optional, only when PREVIEW_PROVIDER=gemini.
# GEMINI_IMAGE_MODEL=gemini-2.5-flash-image
